@model MyApp.Models.ViewModels.CategoryVM

<form method="post" asp-action="CreateUpdate">
    <input type="hidden" asp-for="Category.Id" />
    <div class="border p-3 mt-4">
        <div class="row pb-3">

            <h2 class="text-primary">@((Model.Category == null || Model.Category.Id == 0) ? "Create" : "Update") Category</h2>
            <hr /> 

        </div>
        <div asp-validation-summary=All></div>   @*Server Validation summary for all text fields*@
        <div class="row mb-3">
            <label asp-for="Category.Name"></label>
            <input asp-for="Category.Name" class="form-control" />
            <span asp-validation-for="Category.Name" class="text-danger"></span>     @*Server Validation For specific Text field or Value*@
        </div>

        <div class="row mb-3">
            <label asp-for="Category.DisplayOrder"></label>
            <input asp-for="Category.DisplayOrder" class="form-control" />
            <span asp-validation-for="Category.DisplayOrder" class="text-danger"></span>
        </div>

        @*Here condition to check which button appear*@
        @if((Model.Category == null || Model.Category.Id == 0))
        {
            <button class="btn btn-primary" type="submit">Create</button>
        }
        else
        {
            <button class="btn btn-primary" type="submit">Update</button>
        }

    </div>
    <a asp-action="Index" asp-controller="category" class="btn btn-success mt-5">Back To List</a>

</form>


@*Clinet Side Validation to Display error message without page refresh*@
@section scripts {
    @{
        <partial name="_ValidationScriptsPartial"/>
    }
}